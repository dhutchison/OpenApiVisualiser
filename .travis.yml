language: node_js
node_js:
  - "9"
dist: trusty
sudo: false

#branches:
#  only:
#  - master

cache:
  directories:
    - node_modules

before_script:
  - npm install -g @angular/cli
script:
  - ng lint
  - ng build --prod --output-path docs --base-href /OpenApiVisualiser/
  - cp docs/index.html docs/404.html

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  local_dir: docs
  on:
    branch: master